@extends('master')

@section('title', 'Service')

@section('content')
    <h1>{{ $pageName }}</h1>
    {{-- <ul>
        @foreach ($services as $service)
            <li>{{ $service }}</li>
        @endforeach
    </ul> --}}
@endsection
